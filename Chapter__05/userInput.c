#include <stdio.h>

int main(){
  int num = 0;
  printf("Taking input from the user\n");
  // * scanf is used to take input from the user by using keyboard
  scanf("%d", &num);
  printf("You entered: %d\n", num);
  return 0;
}