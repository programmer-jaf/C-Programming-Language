#include <stdio.h>

int main() {
  printf("Break down the Main program");
  /*
  * #include <stdio.h> --> Preprossor dirctive
  * in every Programming language just like C/C++/Java program file the entry point of the program is Main function/method
  * When the program start then the Main function will be called
  * printf("Break down the main program"); it means that when main function will be called the line ["Break down the main program"] will be printed as an output line
  * return 0 it means the program will end successfully
  */
  return 0;
}